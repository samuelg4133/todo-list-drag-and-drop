import { Container } from "./container";
import { Header } from "./header";
import { HeaderTitle } from "./title";
import { TopButtons } from "./top-buttons";

export const Wrapper = { Root: Container, Header, HeaderTitle, TopButtons };
